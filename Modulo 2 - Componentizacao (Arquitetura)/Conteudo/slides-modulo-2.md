# Módulo 2 - Componentização (Arquitetura)

## 01-Props
- Comunicação Pai -> Filho
- Declaração de Props (`defineProps`)
- Tipagem de Props com TypeScript
- Validação de Props

## 02-Events-Emits
- Comunicação Filho -> Pai
- Declaração de Eventos (`defineEmits`)
- Emissão de Eventos (`emit`)
- Validação de Eventos

## 03-Slots
- Conteúdo Dinâmico em Componentes
- Slots Padrão
- Slots Nomeados (`<slot name="nomeDoSlot">`)
- Scoped Slots (Passando dados do filho para o slot do pai)

## 04-Lifecycle-Hooks
- Ganchos de Ciclo de Vida da Composition API
- `onMounted`
- `onUpdated`
- `onUnmounted`
- Outros Hooks (`onBeforeMount`, `onBeforeUpdate`, `onBeforeUnmount`, `onErrorCaptured`, `onRenderTracked`, `onRenderTriggered`, `onActivated`, `onDeactivated`)

## 05-Model-Binding
- `v-model` em Componentes Personalizados
- `defineModel` (Vue 3.4+)
- Múltiplos `v-model`
