# Módulo 1 - Fundamentos (Core Vue)

## 01-Instalacao-SFC
- Estrutura de Componentes Single File (`.vue`)
- `<script setup>`

## 02-Template-Syntax
- Interpolação de Texto (`{{ }}`)
- Diretivas (`v-bind`, `v-model`, `v-text`, `v-html`, `v-once`, `v-memo`)

## 03-Reatividade
- `ref()` vs `reactive()`
- O Conceito Chave da Reatividade no Vue 3

## 04-Eventos
- `v-on` (Shorthand `@`)
- Modificadores de Eventos (`.stop`, `.prevent`, `.capture`, `.self`, `.once`, `.passive`)

## 05-Condicionais-Listas
- `v-if`, `v-else-if`, `v-else`
- `v-show`
- `v-for` (Iteração de Listas, Importância do `key` attribute)

## 06-Computed-Watchers
- Propriedades Computadas (`computed`)
- Observadores (`watch`, `watchEffect`)
- Diferenças: Side Effects vs Derived State
